# deps
node_modules/
bun.lockb

# build / render outputs
dist/
out/
.vean/
target/
app/src-tauri/gen/
# bundled GPL/LGPL melt/ffmpeg sidecars — a build artifact of scripts/bundle-sidecars.ts,
# assembled into the signed .app only; never commit (47MB binaries, Hard boundary #2).
app/src-tauri/sidecars/
app/src-tauri/package-runtime/
remotion/out/
viewer/dist/
viewer/test-results/
viewer/.vitest-attachments/

# rendered artifacts (corpus keeps .mlt sources, never renders)
*.mp4
*.mov
*.png.tmp

# env / secrets (tract is stateless, but never commit these if they appear)
.env
.env.*

# os / editor
.DS_Store
*.log
coverage/
.vitest/

# local vean projects (documents + comps + outputs; media stays in ~/Github/media)
projects/
# H07 candidate generation proves repeatability and the full alpha export on
# every run. Keep the compact primary stills in git; repeat copies and the
# 17MB export intermediate are reproducible evidence, not source fixtures.
corpus/harness/media/candidate-goldens/runtime/
corpus/harness/media/candidate-goldens/*-repeat-master-*.png
