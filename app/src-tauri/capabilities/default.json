{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default vean app capability. Domain behavior is routed through registered actions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
