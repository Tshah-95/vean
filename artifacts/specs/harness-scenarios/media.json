{
  "schema_version": "1.0.0",
  "runtime": {
    "chrome": "playwright/chromium/headless",
    "wkwebview": "dedicated macOS runner (not configured locally)",
    "host_ui_forbidden": true
  },
  "sections": [
    "live.chrome",
    "live.wkwebview",
    "render.remotion",
    "render.mlt",
    "resilience",
    "performance",
    "performance.release-package",
    "live-export-parity"
  ],
  "scenarios": [
    {
      "id": "media.chrome.ingest-product-path",
      "predicate": "authorized H.264, HEVC, ProRes 422, and ProRes 4444 sources traverse /api/source-proxy, bind persisted source/artifact hashes plus codec/pixfmt/alpha lineage, and decode through FootageStage's ParallelDecoder and FrameCache"
    },
    {
      "id": "media.chrome.proxy-avc",
      "predicate": "the separately declared runtime-proxy cell decodes exact fixture bytes; it cannot substitute for an ingest cell"
    },
    {
      "id": "media.chrome.proxy-vp9-alpha",
      "predicate": "exact alpha proxy yields nonblank pixels with transparent samples"
    },
    {
      "id": "media.chrome.audio",
      "predicate": "PCM, AAC, and MP3 decode to attributed non-silent channel signals"
    },
    {
      "id": "media.chrome.unsupported",
      "predicate": "corrupt video and unsupported audio fail with runtime attribution"
    },
    {
      "id": "media.chrome.buffering",
      "predicate": "delayed bytes remain stalled and never masquerade as readiness"
    },
    {
      "id": "media.resources.lifecycle",
      "predicate": "actual FootageStage scrub/play/seek/unmount balances FrameCache bitmaps, ParallelDecoder workers, AudioGraph context, and GL ownership; mutation removes the real FrameCache.clear cleanup branch"
    },
    {
      "id": "media.webgl.context-loss",
      "predicate": "the actual FootageStage canvas loses/restores its GL context and republishes valid product state"
    },
    {
      "id": "media.chrome.approx-filter-product-fallback",
      "predicate": "a parsed unsupported filter activates FootageStage's approximate state and exactly one real /api/still request produces the exact-still product overlay"
    },
    {
      "id": "media.chrome.alpha-probe-product-failure",
      "predicate": "an authorized unprobeable source traverses the real viewer/server flow and fails with attributed ALPHA_PROBE_UNKNOWN; no opaque proxy is published"
    },
    {
      "id": "media.render.golden",
      "predicate": "approved immutable generated frames match hashes and stable-region policy"
    },
    {
      "id": "media.live-export.parity",
      "predicate": "master/local mapping, document, props, assets, and engine versions remain bound"
    },
    {
      "id": "media.performance.chrome",
      "predicate": "three warmups plus fifteen fresh-process runs retain 300 raw composite samples each"
    },
    {
      "id": "media.wkwebview.matrix",
      "predicate": "the same fixture hashes run through the actual WKWebView product provider; the measured Mediabunny CanvasSink decoder failure remains blocking and cannot be replaced by HTML video or Chrome evidence"
    },
    {
      "id": "media.performance.release-package",
      "predicate": "optimized package distribution is independently measured"
    }
  ],
  "negative_controls": [
    "missing-imagebitmap-close",
    "opaque-alpha-substitution",
    "wrong-frame-timestamp",
    "silent-audio",
    "swapped-audio-channel",
    "unrestored-webgl-context-loss",
    "injected-long-task",
    "unapproved-policy",
    "golden-regeneration-during-acceptance",
    "lower-third-source",
    "one-frame-offset",
    "export-only-props",
    "asset-resolution",
    "modified-golden",
    "boundary-absence"
  ]
}
